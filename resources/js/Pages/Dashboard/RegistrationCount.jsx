import React from 'react';

export default function RegistrationCount({ count, event }) {
    return (
        <div>
            {/* This component is used only for Inertia response */}
            {/* The count will be extracted in the parent component */}
        </div>
    );
}
